# Local prerecorded video (path is resolved from the repository root)
VIDEO_SOURCE=ai/test_videos/road_video.mp4

# Smartphone network camera (use a real OpenCV-compatible URL at runtime)
# VIDEO_SOURCE=<SMARTPHONE_CAMERA_STREAM_URL>

# Verified RAKSHAK RDD2022 model and its target class
MODEL_PATH=ai/model/yolo12s_RDD2022_best.pt
DETECTION_CLASS=D40
CONFIDENCE_THRESHOLD=0.60
